# All instants must come from Runtime::now() so tests can inject a clock.
disallowed-methods = [
    { path = "std::time::Instant::now", reason = "take time via Runtime::now() so tests can inject a mock clock" },
    { path = "web_time::Instant::now", reason = "take time via Runtime::now() so tests can inject a mock clock" },
]
