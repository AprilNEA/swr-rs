[package]
name = "swr-gpui"
description = "GPUI adapter for swr: a Runtime on GPUI's executors and a Query bridge from watch changes to entity notifications"
version = "0.1.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
swr-core.workspace = true
gpui = ">=0.2, <1.0"

[dev-dependencies]
gpui = { version = ">=0.2, <1.0", features = ["test-support"] }
